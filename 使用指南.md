# Aurora 使用指南

## 🎯 快速开始

### 方式一：使用安装包（推荐）

1. **下载安装包**
   - 位置：`src-tauri/target/release/bundle/`
   - 选择以下任一安装包：
     - `msi/Aurora_0.1.0_x64_en-US.msi`
     - `nsis/Aurora_0.1.0_x64-setup.exe`

2. **安装**
   - 双击安装包
   - 按照向导完成安装
   - 安装完成后，在系统托盘找到 Aurora 图标

3. **启动**
   - 右键托盘图标
   - 选择"设置"打开主界面

---

### 方式二：从源代码运行

#### 前置要求
- Node.js 18.0+
- Rust 1.70+
- pnpm 8.0+
- CMake 3.20+
- Visual Studio 2019+ 或 MinGW

#### 步骤

**1. 安装前端依赖**
```bash
cd D:\CodeProject\PBL2\Aurora\Aurora
pnpm install
```

**2. 编译 C++ 后端**
```bash
cd aurora-core
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release

# 复制到 Tauri 目录
copy bin\AuroraCore.exe ..\..\src-tauri\bin\AuroraCore.exe
copy bin\AuroraCore.exe ..\..\src-tauri\bin\AuroraCore-x86_64-pc-windows-msvc.exe
```

**3. 运行开发模式**
```bash
cd ../..
pnpm tauri:dev
```

**4. 构建生产版本**
```bash
pnpm tauri build
```

---

## 📖 功能使用

### 1. 设置静态壁纸

**方法一：通过前端界面**
1. 打开 Aurora 设置面板
2. 导航到"壁纸"页面
3. 选择"静态壁纸"标签
4. 点击"选择图片"按钮
5. 选择你的图片文件
6. 点击"应用"

**方法二：通过代码**
```typescript
import { useWallpaperStore } from '@/stores/wallpaperStore'

const store = useWallpaperStore()

// 设置静态壁纸
await store.setStaticWallpaper('D:/Pictures/wallpaper.jpg')
```

**支持格式：**
- JPG / JPEG
- PNG
- BMP
- WEBP

---

### 2. 设置动态壁纸（视频）

**前置要求：**
- 系统中已安装 ffplay.exe（FFmpeg 的一部分）
- 或者将 ffplay.exe 放在系统 PATH 中

**方法一：通过前端界面**
1. 打开 Aurora 设置面板
2. 导航到"壁纸"页面
3. 选择"动态壁纸"标签
4. 点击"选择视频"按钮
5. 选择你的视频文件
6. 调整音量和循环设置
7. 点击"应用"

**方法二：通过代码**
```typescript
import { useWallpaperStore } from '@/stores/wallpaperStore'

const store = useWallpaperStore()

// 设置视频壁纸
await store.setVideoWallpaper('D:/Videos/wallpaper.mp4', {
  volume: 50,      // 音量 (0-100)
  loop: true       // 是否循环播放
})
```

**支持格式：**
- MP4
- MKV
- AVI
- WEBM

**性能建议：**
- 推荐使用 1080p 或以下分辨率的视频
- 使用 H.264 编码的视频性能更好
- 避免使用过高码率的视频

---

### 3. 停止壁纸

**方法一：通过前端界面**
1. 打开 Aurora 设置面板
2. 导航到"壁纸"页面
3. 点击"停止壁纸"按钮

**方法二：通过代码**
```typescript
import { sendCommand } from '@/composables/useTauriBridge'

// 停止当前壁纸（主要用于动态壁纸）
await sendCommand('stop_wallpaper')
```

---

### 4. 查看当前壁纸信息

```typescript
import { sendCommand } from '@/composables/useTauriBridge'

// 获取当前壁纸信息
await sendCommand('get_current_wallpaper')

// 监听响应
onEvent('current_wallpaper_info', (data) => {
  console.log('当前壁纸路径:', data.path)
})
```

---

## 🔧 高级配置

### 修改 ffplay 路径

如果 ffplay 不在系统 PATH 中，需要修改 C++ 代码：

**文件：** `aurora-core/src/modules/WallpaperModule.cpp`

**位置：** 第 112 行

```cpp
// 修改前
std::wstring commandLine = L"ffplay.exe \"" + wVideoPath + ...

// 修改为（使用绝对路径）
std::wstring commandLine = L"D:\\Tools\\ffmpeg\\bin\\ffplay.exe \"" + wVideoPath + ...
```

修改后需要重新编译 C++ 后端。

---

### 自定义壁纸样式

静态壁纸支持多种样式，可以修改代码设置：

**文件：** `aurora-core/src/modules/WallpaperModule.cpp`

**位置：** 第 35 行

```cpp
// 壁纸样式选项
// 0  = 居中
// 2  = 拉伸
// 6  = 适应
// 10 = 填充（当前默认）
// 22 = 跨区（多显示器）

const wchar_t* style = L"10";  // 修改这里
```

---

## 🐛 常见问题

### 1. 动态壁纸无法播放

**问题：** 视频壁纸设置后没有显示

**解决方案：**
1. 检查 ffplay 是否已安装
   ```bash
   ffplay -version
   ```
2. 确保视频文件格式被支持
3. 查看 Sidecar 日志输出
4. 尝试使用其他视频文件

---

### 2. 找不到 SDL 窗口

**问题：** 日志显示 "Failed to find SDL window"

**解决方案：**
1. 增加等待时间（修改代码中的 Sleep 时间）
2. 检查 ffplay 版本是否兼容
3. 尝试重启应用

---

### 3. 静态壁纸设置失败

**问题：** 壁纸没有改变

**解决方案：**
1. 检查图片文件是否存在
2. 确保图片格式被支持
3. 检查文件路径是否正确（使用绝对路径）
4. 查看错误日志

---

### 4. Sidecar 进程启动失败

**问题：** 应用无法与 C++ 后端通信

**解决方案：**
1. 检查 `src-tauri/bin/` 目录下是否有：
   - `AuroraCore.exe`
   - `AuroraCore-x86_64-pc-windows-msvc.exe`
2. 确保两个文件都存在且完整
3. 重新编译 C++ 后端

---

## 📊 日志和调试

### 查看日志

**前端日志：**
- 打开 DevTools (F12)
- 查看 Console 标签

**Sidecar 日志：**
- 在控制台中查看标准输出
- 使用开发模式运行以查看详细日志

**日志标签说明：**
```
[Tauri Bridge] - 前端与后端通信
[INFO]         - C++ 后端信息
C++ Error:     - C++ 后端错误
```

---

### 开发模式调试

```bash
# 启动开发模式
pnpm tauri:dev

# 日志会在控制台实时输出
# 包括：
# - Sidecar 启动信息
# - 命令发送和接收
# - 事件触发
# - 错误信息
```

---

## 💡 使用技巧

### 1. 创建壁纸收藏夹
将常用壁纸路径保存到配置文件中，快速切换。

### 2. 定时更换壁纸
使用 Windows 任务计划程序定时调用 Aurora API。

### 3. 多显示器支持
- 静态壁纸会应用到所有显示器
- 动态壁纸当前仅支持主显示器（未来会扩展）

### 4. 性能优化
- 静态壁纸几乎无性能开销
- 动态壁纸建议：
  - 使用硬件加速的视频编码
  - 关闭音频（设置音量为 0）
  - 使用较低分辨率

---

## 📞 获取帮助

### 文档
- [开发完成报告](开发完成报告.md)
- [详细开发文档](docs/Aurora开发文档（详细版）.md)
- [README](README.md)

### 问题反馈
如遇到问题，请提供：
1. 操作系统版本
2. Aurora 版本号
3. 错误日志
4. 复现步骤

---

## 🎉 享受使用

现在你已经掌握了 Aurora 的基本使用方法！

尽情享受你的个性化桌面吧！ ✨

---

**版本：** v0.1.0  
**更新时间：** 2025-10-30  
**适用平台：** Windows 10/11

