# NPM 配置文件
# 禁用不必要的警告

# 忽略未知环境配置警告
ignore-scripts=false

# 使用 pnpm 作为默认包管理器
engine-strict=false

# 其他优化配置
loglevel=warn

# 禁用已废弃的配置项警告（这些配置项在新版本 npm 中已不再支持）
# 通过将它们设置为空或默认值来避免警告
update-notifier=true

