# 分辨率问题诊断指南

## 🔍 问题诊断

如果动态壁纸的分辨率仍然不对，请按以下步骤诊断：

---

## 步骤 1: 检查你的真实屏幕分辨率

### 方法 A: Windows 显示设置
1. 右键桌面 → **显示设置**
2. 查看 **"显示分辨率"**
3. 查看 **"缩放与布局"**

**记录：**
```
显示分辨率: _______ x _______
缩放比例: _______% 
```

### 方法 B: 使用 PowerShell 查询
```powershell
# 运行这个命令查看真实分辨率
Add-Type @"
using System;
using System.Runtime.InteropServices;
public class Display {
    [DllImport("user32.dll")]
    public static extern bool EnumDisplaySettings(string deviceName, int modeNum, ref DEVMODE devMode);
    [StructLayout(LayoutKind.Sequential)]
    public struct DEVMODE {
        [MarshalAs(UnmanagedType.ByValTStr, SizeConst = 32)]
        public string dmDeviceName;
        public short dmSpecVersion;
        public short dmDriverVersion;
        public short dmSize;
        public short dmDriverExtra;
        public int dmFields;
        public int dmPositionX;
        public int dmPositionY;
        public int dmDisplayOrientation;
        public int dmDisplayFixedOutput;
        public short dmColor;
        public short dmDuplex;
        public short dmYResolution;
        public short dmTTOption;
        public short dmCollate;
        [MarshalAs(UnmanagedType.ByValTStr, SizeConst = 32)]
        public string dmFormName;
        public short dmLogPixels;
        public int dmBitsPerPel;
        public int dmPelsWidth;
        public int dmPelsHeight;
        public int dmDisplayFlags;
        public int dmDisplayFrequency;
    }
}
"@

$devMode = New-Object Display+DEVMODE
$devMode.dmSize = [Runtime.InteropServices.Marshal]::SizeOf($devMode)

if ([Display]::EnumDisplaySettings($null, -1, [ref]$devMode)) {
    Write-Host "=== 真实物理分辨率 ===" -ForegroundColor Green
    Write-Host "宽度: $($devMode.dmPelsWidth)"
    Write-Host "高度: $($devMode.dmPelsHeight)"
    Write-Host "刷新率: $($devMode.dmDisplayFrequency) Hz"
    Write-Host "色深: $($devMode.dmBitsPerPel) bits"
}
```

---

## 步骤 2: 启动应用并查看详细日志

```bash
cd d:\CodeProject\PBL2\Aurora\Aurora
pnpm tauri:dev
```

**打开 DevTools (F12)** 查看控制台

---

## 步骤 3: 设置动态壁纸

1. 选择 "视频壁纸"
2. 选择一个视频文件
3. 点击 "应用壁纸"

---

## 步骤 4: 查看日志输出

### 你应该看到类似这样的详细日志：

```
=== Display Settings (Physical Resolution) ===
Resolution: 1920x1080
Refresh Rate: 60 Hz
Bits Per Pixel: 32

DPI: 120x120 (Standard: 96)
DPI Scale: 125%
Logical Pixels (GetSystemMetrics): 1536x864
Physical Pixels (EnumDisplaySettings): 1920x1080

=== Final Video Resolution: 1920x1080 ===
```

### 关键信息对比：

| 项目 | 你的值 | 应该是什么 |
|-----|--------|----------|
| **Physical Pixels** | _________ | 你的真实屏幕分辨率 |
| **Logical Pixels** | _________ | 受DPI缩放影响的值 |
| **DPI Scale** | _________ | 你的缩放比例 |
| **Final Video Resolution** | _________ | 应该等于 Physical Pixels |

---

## 步骤 5: 验证诊断

### ✅ 如果日志显示正确：

```
=== Final Video Resolution: 1920x1080 ===  ← 和你的真实分辨率一致
```

**但视频仍然不对，可能原因：**
1. ffplay 窗口大小设置正确，但窗口放置位置不对
2. 多显示器环境
3. 视频本身的宽高比问题

**继续到步骤 6**

### ❌ 如果日志显示错误：

```
=== Final Video Resolution: 1536x864 ===  ← 这是逻辑像素，不对！
```

**说明 EnumDisplaySettings 也受到了影响，需要特殊处理**

**继续到步骤 7**

---

## 步骤 6: 检查 ffplay 窗口

### 查找 SDL 窗口信息

在 PowerShell 中运行：
```powershell
# 查找 ffplay 进程
Get-Process | Where-Object {$_.ProcessName -like "*ffplay*"} | Format-List

# 使用 Spy++ 或类似工具查看窗口大小
```

### 预期：
- 窗口大小应该是你的屏幕物理分辨率
- 窗口位置应该在 (0, 0)

---

## 步骤 7: 高级诊断 - API 测试

创建测试文件 `test_resolution.cpp`:

```cpp
#include <windows.h>
#include <iostream>

int main() {
    std::cout << "=== 分辨率诊断工具 ===" << std::endl;
    
    // 1. GetSystemMetrics (逻辑像素)
    int sm_width = GetSystemMetrics(SM_CXSCREEN);
    int sm_height = GetSystemMetrics(SM_CYSCREEN);
    std::cout << "\n1. GetSystemMetrics (逻辑):" << std::endl;
    std::cout << "   " << sm_width << "x" << sm_height << std::endl;
    
    // 2. EnumDisplaySettings (物理像素)
    DEVMODEW devMode = {0};
    devMode.dmSize = sizeof(devMode);
    if (EnumDisplaySettingsW(NULL, ENUM_CURRENT_SETTINGS, &devMode)) {
        std::cout << "\n2. EnumDisplaySettings (物理):" << std::endl;
        std::cout << "   " << devMode.dmPelsWidth << "x" << devMode.dmPelsHeight << std::endl;
        std::cout << "   刷新率: " << devMode.dmDisplayFrequency << " Hz" << std::endl;
    }
    
    // 3. GetMonitorInfo
    HWND hDesktop = GetDesktopWindow();
    HMONITOR hMonitor = MonitorFromWindow(hDesktop, MONITOR_DEFAULTTOPRIMARY);
    MONITORINFO monitorInfo = {0};
    monitorInfo.cbSize = sizeof(MONITORINFO);
    if (GetMonitorInfoW(hMonitor, &monitorInfo)) {
        int w = monitorInfo.rcMonitor.right - monitorInfo.rcMonitor.left;
        int h = monitorInfo.rcMonitor.bottom - monitorInfo.rcMonitor.top;
        std::cout << "\n3. GetMonitorInfo:" << std::endl;
        std::cout << "   " << w << "x" << h << std::endl;
    }
    
    // 4. DPI 信息
    HDC hdc = GetDC(NULL);
    if (hdc) {
        int dpiX = GetDeviceCaps(hdc, LOGPIXELSX);
        int dpiY = GetDeviceCaps(hdc, LOGPIXELSY);
        std::cout << "\n4. DPI 信息:" << std::endl;
        std::cout << "   DPI: " << dpiX << "x" << dpiY << std::endl;
        std::cout << "   缩放: " << (dpiX * 100 / 96) << "%" << std::endl;
        ReleaseDC(NULL, hdc);
    }
    
    std::cout << "\n=== 诊断完成 ===" << std::endl;
    system("pause");
    return 0;
}
```

**编译并运行：**
```bash
g++ test_resolution.cpp -o test_resolution.exe -lgdi32 -luser32
./test_resolution.exe
```

---

## 步骤 8: 解决方案

### 方案 A: 如果 EnumDisplaySettings 返回正确的值

**问题可能在 ffplay 或窗口放置**

修改 `WallpaperModule.cpp` 添加调试：
```cpp
std::cout << "ffplay command: " << commandLine << std::endl;
std::cout << "Window handle: " << hFfplay << std::endl;
std::cout << "Screen bounds: " << bounds.left << "," << bounds.top 
          << " to " << bounds.right << "," << bounds.bottom << std::endl;
```

### 方案 B: 如果所有 API 都返回错误的值

**你的系统可能需要应用程序清单文件声明 DPI-Aware**

创建 `AuroraCore.exe.manifest`:
```xml
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <dpiAware xmlns="http://schemas.microsoft.com/SMI/2005/WindowsSettings">true</dpiAware>
      <dpiAwareness xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">PerMonitorV2</dpiAwareness>
    </windowsSettings>
  </application>
</assembly>
```

**在 CMakeLists.txt 中添加：**
```cmake
if(MSVC)
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} /MANIFEST:EMBED /MANIFESTINPUT:${CMAKE_SOURCE_DIR}/AuroraCore.exe.manifest")
endif()
```

### 方案 C: 使用注册表强制获取

```cpp
HKEY hKey;
if (RegOpenKeyExW(HKEY_CURRENT_CONFIG, 
    L"System\\CurrentControlSet\\Control\\VIDEO\\{GUID}\\0000", 
    0, KEY_READ, &hKey) == ERROR_SUCCESS) {
    // 读取注册表中的真实分辨率
    DWORD width, height;
    DWORD size = sizeof(DWORD);
    RegQueryValueExW(hKey, L"DefaultSettings.XResolution", NULL, NULL, (LPBYTE)&width, &size);
    RegQueryValueExW(hKey, L"DefaultSettings.YResolution", NULL, NULL, (LPBYTE)&height, &size);
    RegCloseKey(hKey);
}
```

---

## 📊 诊断报告模板

请运行测试后填写：

```
=== Aurora 分辨率诊断报告 ===

【系统信息】
Windows 版本: _______________
显卡: _______________

【显示设置】
显示分辨率 (Windows设置): _______ x _______
缩放比例: _______%

【API 测试结果】
GetSystemMetrics: _______ x _______
EnumDisplaySettings: _______ x _______
GetMonitorInfo: _______ x _______
DPI: _______ x _______ (缩放: _____%)

【Aurora 日志】
Physical Pixels: _______ x _______
Logical Pixels: _______ x _______
Final Video Resolution: _______ x _______

【问题描述】
视频实际显示效果: 
□ 完全填充
□ 有黑边
□ 被拉伸
□ 被压缩
□ 其他: _______________

【截图】
(如果可能，请附上截图)
```

---

## 🔧 快速修复命令

如果确定是 DPI 问题，尝试：

```bash
# 1. 临时关闭 DPI 缩放测试
# 右键 AuroraCore.exe → 属性 → 兼容性
# 勾选 "替代高 DPI 缩放行为"
# 选择 "系统(增强)"

# 2. 重新编译
cd aurora-core/build
cmake --build . --config Release

# 3. 复制文件
Copy-Item "bin\AuroraCore.exe" "..\..\src-tauri\bin\" -Force
Copy-Item "bin\AuroraCore.exe" "..\..\src-tauri\bin\AuroraCore-x86_64-pc-windows-msvc.exe" -Force
```

---

## 💡 下一步

完成诊断后，请提供：

1. **诊断报告** (上面的模板)
2. **控制台日志** (完整的)
3. **你的实际分辨率和缩放**
4. **问题截图** (如果可能)

这样我可以提供更精确的解决方案！

---

**创建时间:** 2025-10-31  
**用途:** 诊断和解决分辨率/DPI问题

