# Aurora 开发指南

## 📖 目录

1. [快速开始](#快速开始)
2. [开发环境配置](#开发环境配置)
3. [项目架构](#项目架构)
4. [开发工作流](#开发工作流)
5. [核心模块开发](#核心模块开发)
6. [调试与测试](#调试与测试)
7. [构建与部署](#构建与部署)
8. [常见问题](#常见问题)

---

## 快速开始

### 前置要求

- **Node.js** 18.0+
- **Rust** 1.70+
- **pnpm** 8.0+
- **CMake** 3.20+
- **Visual Studio 2019+** (Windows)

### 克隆项目

```bash
git clone https://github.com/MitsuhaFe/aurora.git
cd aurora
```

### 安装依赖

```bash
# 安装前端依赖
pnpm install
```

### 启动开发模式

```bash
# 方式 1：完整应用（推荐）
pnpm tauri:dev

# 方式 2：仅前端开发
pnpm dev
```

**首次启动需要 2-5 分钟编译 Rust 代码。**

---

## 开发环境配置

### 前端开发环境

#### 必需软件

1. **Node.js 18.0+**
   ```bash
   node --version  # 应显示 v18.x.x 或更高
   npm --version   # 应显示 9.x.x 或更高
   ```

2. **pnpm（推荐）**
   ```bash
   npm install -g pnpm
   ```

3. **Rust 工具链**
   ```bash
   # Windows: 访问 https://rustup.rs/ 下载安装
   # 验证安装
   rustc --version
   cargo --version
   ```

4. **VS Code 推荐插件**
   - Vue Language Features (Volar)
   - TypeScript Vue Plugin (Volar)
   - ESLint
   - Prettier
   - Tauri

### C++ 开发环境（Windows）

1. **Visual Studio 2019/2022**
   - 安装 "使用 C++ 的桌面开发" 工作负载
   - 确保包含 CMake 工具

2. **CMake 3.20+**
   ```bash
   cmake --version
   ```

3. **vcpkg（C++ 包管理器）**
   ```bash
   git clone https://github.com/microsoft/vcpkg.git
   cd vcpkg
   .\bootstrap-vcpkg.bat
   .\vcpkg integrate install
   ```

4. **依赖库安装**
   ```bash
   # 使用 vcpkg 安装
   .\vcpkg install nlohmann-json:x64-windows
   .\vcpkg install cpr:x64-windows
   ```

---

## 项目架构

### 目录结构

```
Aurora/
├── src/                        # Vue 3 前端源代码
│   ├── views/                  # 页面组件
│   │   ├── Home.vue
│   │   ├── Settings/
│   │   ├── Dock.vue
│   │   └── Pet.vue
│   ├── components/             # 可复用组件
│   ├── stores/                 # Pinia 状态管理
│   │   ├── appStore.ts
│   │   ├── wallpaperStore.ts
│   │   └── dockStore.ts
│   ├── composables/            # 组合式函数
│   │   └── useTauriBridge.ts  # IPC 通信桥
│   ├── router/                 # 路由配置
│   ├── styles/                 # 全局样式
│   └── types/                  # TypeScript 类型定义
│
├── src-tauri/                  # Tauri 配置与 Rust 代码
│   ├── src/main.rs             # Rust 入口
│   ├── tauri.conf.json         # Tauri 配置
│   ├── Cargo.toml              # Rust 依赖
│   └── bin/                    # Sidecar 可执行文件
│       └── AuroraCore.exe
│
├── aurora-core/                # C++ 后端源代码
│   ├── CMakeLists.txt          # CMake 配置
│   ├── src/
│   │   ├── main.cpp            # 入口和命令循环
│   │   ├── CommandHandler.cpp  # 命令处理器
│   │   └── modules/            # 功能模块
│   │       ├── wallpaper/      # 壁纸模块
│   │       ├── dock/           # Dock 模块
│   │       ├── widget/         # 小组件模块
│   │       └── pet/            # 桌面伙伴模块
│   └── build/                  # 构建输出目录
│
├── docs/                       # 项目文档
├── public/                     # 公共资源
├── scripts/                    # 构建脚本
├── package.json
├── vite.config.ts
└── README.md
```

### 技术栈

**前端：**
- Vue 3.3 (Composition API)
- TypeScript 5.0
- Vite 5.0
- Pinia 2.0
- Vue Router 4.0

**容器：**
- Tauri 1.5
- WebView2 (Windows)

**后端：**
- C++17
- CMake
- nlohmann/json
- libmpv（视频播放）
- Windows API

### 架构设计

```
┌─────────────────────────────────────┐
│      Tauri 容器 (WebView2)          │
│  ┌───────────────────────────────┐  │
│  │   Vue 3 前端 (UI Layer)       │  │
│  │  • 设置界面                   │  │
│  │  • Dock 窗口                  │  │
│  │  • 桌面伙伴                   │  │
│  │  • 小组件                     │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
         ↕ IPC (JSON via stdin/stdout)
┌─────────────────────────────────────┐
│   C++ 后端 (AuroraCore.exe)         │
│  • 壁纸模块 (WallpaperModule)       │
│  • Dock 模块 (DockModule)           │
│  • 小组件模块 (WidgetModule)        │
│  • 桌面伙伴模块 (PetModule)         │
└─────────────────────────────────────┘
         ↕
┌─────────────────────────────────────┐
│      操作系统 API (Windows API)      │
└─────────────────────────────────────┘
```

---

## 开发工作流

### 前端开发

#### 1. 创建新组件

```bash
# 在 src/components/ 下创建组件
src/components/MyComponent.vue
```

```vue
<script setup lang="ts">
import { ref } from 'vue'

const count = ref(0)
</script>

<template>
  <div class="my-component">
    <button @click="count++">Count: {{ count }}</button>
  </div>
</template>

<style scoped>
.my-component {
  padding: 20px;
}
</style>
```

#### 2. 添加路由

编辑 `src/router/index.ts`：

```typescript
const routes = [
  {
    path: '/my-page',
    name: 'MyPage',
    component: () => import('@/views/MyPage.vue')
  }
]
```

#### 3. 使用状态管理

```typescript
// src/stores/myStore.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useMyStore = defineStore('my-store', () => {
  const data = ref<string[]>([])
  
  async function fetchData() {
    // 实现数据获取逻辑
  }
  
  return { data, fetchData }
})
```

#### 4. IPC 通信

```typescript
import { sendCommand, onEvent } from '@/composables/useTauriBridge'

// 发送命令到 C++ 后端
await sendCommand('my_command', { param: 'value' })

// 监听事件
const unlisten = onEvent('my_event', (data) => {
  console.log('Received:', data)
})
```

### C++ 后端开发

#### 1. 创建新模块

```cpp
// aurora-core/src/modules/mymodule/MyModule.h
#pragma once
#include <string>
#include <nlohmann/json.hpp>

class MyModule {
public:
    MyModule();
    ~MyModule();
    
    void doSomething(const std::string& param);
    nlohmann::json getData();
    
private:
    // 私有成员
};
```

```cpp
// aurora-core/src/modules/mymodule/MyModule.cpp
#include "MyModule.h"
#include <iostream>

MyModule::MyModule() {
    std::cout << "MyModule initialized" << std::endl;
}

MyModule::~MyModule() {
    // 清理资源
}

void MyModule::doSomething(const std::string& param) {
    // 实现功能
}

nlohmann::json MyModule::getData() {
    nlohmann::json result;
    result["data"] = "value";
    return result;
}
```

#### 2. 注册命令处理

编辑 `aurora-core/src/CommandHandler.cpp`：

```cpp
json CommandHandler::handleCommand(const json& command) {
    std::string action = command.value("action", "");
    json payload = command.value("payload", json::object());
    
    json response;
    
    if (action == "my_command") {
        std::string param = payload["param"];
        myModule->doSomething(param);
        response["event"] = "my_event";
        response["data"] = myModule->getData();
    }
    
    return response;
}
```

#### 3. 构建 C++ 后端

```bash
cd aurora-core
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release

# 复制到 Tauri 目录
copy bin\AuroraCore.exe ..\..\src-tauri\bin\
copy bin\AuroraCore.exe ..\..\src-tauri\bin\AuroraCore-x86_64-pc-windows-msvc.exe
```

---

## 核心模块开发

### 壁纸模块

**功能：** 设置静态/动态壁纸

**主要文件：** `aurora-core/src/modules/wallpaper/WallpaperModule.cpp`

**命令：**
- `set_static_wallpaper` - 设置静态壁纸
- `set_dynamic_wallpaper` - 设置视频壁纸
- `stop_wallpaper` - 停止壁纸

**示例：**
```typescript
// 设置静态壁纸
await sendCommand('set_static_wallpaper', {
  path: 'D:/Pictures/wallpaper.jpg'
})

// 设置视频壁纸
await sendCommand('set_dynamic_wallpaper', {
  path: 'D:/Videos/wallpaper.mp4'
})
```

### Dock 模块

**功能：** 应用启动器

**主要文件：** `src/views/Dock.vue`

**特性：**
- 显示运行中的应用
- 固定/取消固定应用
- 鼠标悬停放大动画
- 右键菜单

### 小组件模块

**功能：** 桌面小组件

**支持的小组件：**
- 时钟小组件
- 天气小组件
- 系统监控小组件
- 待办事项小组件

### 桌面伙伴模块

**功能：** 可爱的桌面伙伴

**特性：**
- 可拖拽移动
- 精灵图动画
- 基础交互
- 位置记忆

---

## 调试与测试

### 前端调试

#### 使用 DevTools

```bash
# 启动开发模式
pnpm tauri:dev

# 打开 DevTools
按 F12 或 右键 -> 检查
```

#### Vue DevTools

安装 Vue DevTools 浏览器扩展以调试组件状态。

### C++ 后端调试

#### 查看日志

C++ 后端的日志会输出到控制台（stdout/stderr）。

#### 手动测试

```bash
# 直接运行 Sidecar
cd src-tauri/bin
.\AuroraCore.exe

# 输入 JSON 命令
{"action":"set_static_wallpaper","path":"D:/test.jpg"}
```

#### 使用调试器

在 Visual Studio 中：
1. 打开 `aurora-core` 项目
2. 设置断点
3. 按 F5 启动调试

### 运行测试

```bash
# 前端单元测试
pnpm test

# 端到端测试
pnpm test:e2e

# 代码检查
pnpm lint
```

---

## 构建与部署

### 开发构建

```bash
# 构建前端
pnpm build

# 构建 Tauri 应用（开发模式）
pnpm tauri dev
```

### 生产构建

```bash
# 1. 构建 C++ 后端
cd aurora-core/build
cmake --build . --config Release

# 2. 复制 Sidecar
copy bin\AuroraCore.exe ..\..\src-tauri\bin\
copy bin\AuroraCore.exe ..\..\src-tauri\bin\AuroraCore-x86_64-pc-windows-msvc.exe

# 3. 构建 Tauri 应用
cd ../..
pnpm tauri build
```

### 构建产物

构建完成后，安装包位于：

```
src-tauri/target/release/bundle/
├── msi/
│   └── Aurora_0.1.0_x64_en-US.msi
└── nsis/
    └── Aurora_0.1.0_x64-setup.exe
```

---

## 常见问题

### 1. 端口被占用

**问题：** `Port 5173 is already in use`

**解决：**
```bash
# 查找进程
netstat -ano | findstr :5173

# 关闭进程
taskkill /F /PID <进程ID>
```

### 2. Rust 编译失败

**问题：** Cargo 编译错误

**解决：**
```bash
# 清理缓存
cargo clean

# 更新工具链
rustup update

# 重新构建
pnpm tauri dev
```

### 3. C++ 编译失败

**问题：** CMake 配置或编译错误

**解决：**
```bash
# 清理构建目录
cd aurora-core/build
rm -rf *

# 重新配置
cmake .. -DCMAKE_BUILD_TYPE=Release

# 重新构建
cmake --build . --config Release
```

### 4. Sidecar 进程未启动

**问题：** 无法与 C++ 后端通信

**解决：**
1. 确认 `src-tauri/bin/` 目录下有两个文件：
   - `AuroraCore.exe`
   - `AuroraCore-x86_64-pc-windows-msvc.exe`
2. 检查文件权限
3. 重新复制 Sidecar

### 5. 动态壁纸无法播放

**问题：** 视频壁纸不显示

**解决：**
1. 确认已安装 FFmpeg
   ```bash
   ffplay -version
   ```
2. 将 `ffplay.exe` 添加到 PATH
3. 或修改 `WallpaperModule.cpp` 使用绝对路径

---

## 代码规范

### TypeScript/Vue

- 使用 ESLint 和 Prettier
- 组件文件：PascalCase（`MyComponent.vue`）
- Composables：camelCase with `use` prefix（`useMyThing.ts`）
- 类型注解：始终添加类型

### C++

- 遵循 Google C++ Style Guide
- 类名：PascalCase（`MyClass`）
- 函数名：camelCase（`doSomething`）
- 成员变量：snake_case with trailing underscore（`my_var_`）

### Commit 规范

使用 Conventional Commits：

```bash
feat(dock): add app pinning feature
fix(wallpaper): resolve memory leak
docs(readme): update installation guide
```

---

## 资源链接

- [Vue 3 文档](https://cn.vuejs.org/)
- [Tauri 文档](https://tauri.app/zh-cn/)
- [TypeScript 文档](https://www.typescriptlang.org/zh/)
- [CMake 文档](https://cmake.org/documentation/)

---

**最后更新：** 2025-11-11
**版本：** v0.1.0
