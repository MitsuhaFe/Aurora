# 问题解决方案

## ✅ 已解决的问题

### 1. 端口 5173 被占用

**问题：**
```
Error: Port 5173 is already in use
```

**原因：** 之前的 Vite 开发服务器进程还在运行

**解决方法：** ✅ 已自动关闭进程

**手动解决方法（如果再次遇到）：**

```powershell
# 方法 1：查找并关闭进程
netstat -ano | findstr :5173
taskkill /F /PID <进程ID>

# 方法 2：使用准备好的脚本
.\scripts\kill-port.ps1

# 方法 3：关闭所有 node 进程（谨慎使用）
taskkill /F /IM node.exe
```

---

### 2. npm 环境配置警告

**警告信息：**
```
npm warn Unknown env config "verify-deps-before-run"
npm warn Unknown env config "_jsr-registry"
```

**原因：** npm 环境配置中有不识别的配置项

**解决方法：** ✅ 已创建 `.npmrc` 配置文件

这些警告**不影响功能**，可以安全忽略。已通过配置文件优化显示。

---

## 🚀 现在可以运行了

```bash
# 确认端口已释放
netstat -ano | findstr :5173

# 启动开发服务器
pnpm tauri dev
```

---

## 📝 快速故障排除

### 如果端口仍然被占用

```powershell
# 1. 查找进程
netstat -ano | findstr :5173

# 2. 关闭进程（替换 <PID> 为实际进程 ID）
taskkill /F /PID <PID>

# 3. 重新运行
pnpm tauri dev
```

### 如果其他端口冲突

可以修改 `vite.config.ts` 更改端口：

```typescript
export default defineConfig({
  server: {
    port: 5174,  // 改为其他端口
    strictPort: false,  // 允许自动查找可用端口
  },
})
```

---

## ✨ 预期结果

运行 `pnpm tauri dev` 后应该看到：

```
✓ VITE v5.4.21  ready in XXX ms
✓ Local:   http://localhost:5173/
✓ Compiling aurora-desktop v0.1.0
✓ Finished dev [unoptimized + debuginfo]
```

然后 Aurora 窗口会打开，**直接显示设置页面**（无欢迎界面）。

---

## 🔧 实用脚本

### 快速清理端口

创建的脚本 `scripts/kill-port.ps1` 可以快速清理端口：

```powershell
# 运行脚本
.\scripts\kill-port.ps1

# 然后启动
pnpm tauri dev
```

### 完全重启开发环境

```powershell
# 1. 关闭所有 node 进程
taskkill /F /IM node.exe

# 2. 清理缓存（可选）
pnpm store prune

# 3. 重新启动
pnpm tauri dev
```

---

## 📊 问题状态

| 问题 | 状态 | 解决方法 |
|------|------|----------|
| 端口 5173 占用 | ✅ 已解决 | 关闭进程 ID 7988, 19592 |
| npm 警告 | ✅ 已优化 | 创建 .npmrc 配置 |
| 欢迎页面移除 | ✅ 已完成 | 路由已更新 |

---

## 🎯 下一步

现在所有问题都已解决，可以：

1. ✅ 运行 `pnpm tauri dev`
2. ✅ 查看直接进入设置页面的效果
3. ✅ 开始开发新功能

---

**更新时间：** 2024-10-30  
**所有问题已解决！** 🎉

