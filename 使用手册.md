# Aurora 使用手册

## 📖 目录

1. [安装](#安装)
2. [快速开始](#快速开始)
3. [功能使用](#功能使用)
4. [设置与配置](#设置与配置)
5. [快捷键](#快捷键)
6. [常见问题](#常见问题)
7. [故障排除](#故障排除)

---

## 安装

### 方式一：使用安装包（推荐）

1. **下载安装包**
   - 访问 [Releases 页面](https://github.com/MitsuhaFe/aurora/releases)
   - 下载最新版本：
     - `Aurora_0.1.0_x64_en-US.msi`（MSI 安装包）
     - `Aurora_0.1.0_x64-setup.exe`（NSIS 安装程序）

2. **安装**
   - 双击安装包
   - 按照向导完成安装
   - 安装完成后，在系统托盘找到 Aurora 图标

3. **启动**
   - 右键托盘图标
   - 选择"设置"打开主界面

### 方式二：从源代码运行

#### 前置要求
- Node.js 18.0+
- Rust 1.70+
- pnpm 8.0+
- CMake 3.20+
- Visual Studio 2019+ 或 MinGW

#### 安装步骤

```bash
# 1. 克隆仓库
git clone https://github.com/MitsuhaFe/aurora.git
cd aurora

# 2. 安装前端依赖
pnpm install

# 3. 编译 C++ 后端（如果需要）
cd aurora-core/build
cmake --build . --config Release

# 4. 复制 Sidecar
copy bin\AuroraCore.exe ..\..\src-tauri\bin\

# 5. 启动开发模式
cd ../..
pnpm tauri:dev
```

---

## 快速开始

### 首次使用

1. **启动应用**
   - 从开始菜单或桌面图标启动 Aurora
   - 或右键系统托盘图标 → 设置

2. **浏览设置界面**
   - 左侧导航栏包含所有功能模块
   - 点击不同选项卡切换功能

3. **设置你的第一张壁纸**
   - 点击左侧"壁纸" 🖼️
   - 选择"静态图片"
   - 点击"选择文件"
   - 选择一张图片
   - 点击"应用壁纸"

---

## 功能使用

### 1. 壁纸系统

#### 静态壁纸

**功能：** 设置静态图片作为桌面壁纸

**支持格式：** JPG, PNG, BMP, WEBP

**使用步骤：**
1. 打开 Aurora 设置
2. 点击"壁纸"
3. 确认选择"静态图片"
4. 点击"选择文件"
5. 浏览并选择图片
6. 点击"应用壁纸"

**提示：**
- 推荐使用 1920x1080 或更高分辨率
- 支持透明背景的 PNG 图片
- 壁纸会自动适应屏幕尺寸

#### 动态壁纸（视频）

**功能：** 播放视频作为桌面壁纸

**支持格式：** MP4, MKV, AVI, WEBM

**前置要求：** 需要安装 FFmpeg

**使用步骤：**
1. 打开壁纸设置
2. 切换到"视频壁纸"
3. 点击"选择文件"
4. 选择视频文件
5. 点击"应用壁纸"
6. 等待 1-2 秒加载

**提示：**
- 推荐使用 1080p 或 720p 视频
- 视频会自动循环播放
- 默认静音（保护隐私）
- 支持硬件加速

#### 停止壁纸

如需恢复系统默认壁纸或停止视频播放：

1. 点击"停止壁纸"按钮
2. 或设置新的壁纸（自动停止旧的）

### 2. Dock 栏

**功能：** 类似 macOS 的应用启动器

#### 基本使用

**显示/隐藏 Dock：**
- 快捷键：`Ctrl + Alt + D`
- 或从设置面板控制

**应用管理：**
- **运行中的应用**：自动显示在 Dock 中
- **固定应用**：右键应用图标 → 固定到 Dock
- **取消固定**：右键固定的应用 → 取消固定
- **启动应用**：单击图标
- **关闭应用**：右键 → 退出

#### Dock 设置

在"Dock 栏"设置面板可以调整：

**位置：**
- 底部（默认）
- 左侧
- 右侧
- 顶部

**大小与动画：**
- 图标大小：32-128 像素
- 放大倍数：1.0x - 2.5x
- 动画效果：弹簧、渐隐、涟漪等
- 自动隐藏：启用/禁用

**外观：**
- 背景透明度：0-100%
- 图标透明度：0-100%
- 背景模糊：启用/禁用
- 圆角半径：0-30px

### 3. 桌面伙伴

**功能：** 导入 3D 模型作为桌面伙伴，播放自定义动画并响应桌面事件，后续可扩展 AI 对话、语音与摄像头动捕。

#### 导入与启用桌面伙伴

1. 点击左侧「桌面伙伴」 🐱
2. 打开「启用桌面伙伴」开关
3. 点击「导入模型」，选择 VRM/GLB 等 3D 模型文件
4. 在预览区调整缩放、位置与朝向，确认渲染效果
5. 绑定待机/互动/场景动画（可上传自定义动画文件）
6. 保存设置，桌面伙伴随即出现在桌面

#### 动画与场景

- **待机动画**：伙伴静置时播放的默认动作，可设置循环方式
- **拖拽与吸附**：拖动伙伴靠近屏幕边缘、任务栏或窗口时自动吸附并触发场景动画
- **事件响应**：支持鼠标点击、悬停、系统通知或自定义事件触发指定动画
- **过渡控制**：动画切换可配置淡入/淡出时间，确保动作衔接自然
- **日志追踪**：场景触发与动画切换会记录到调试面板，便于排查

#### 设置项

**模型设置：**
- 模型文件：VRM/GLB 路径（可收藏常用模型）
- 缩放比例：0.2x - 5.0x，支持 DPI 自适应
- 初始位置与旋转：记忆上次位置，可重置为默认
- 材质与光照：可选环境光强度与阴影开关

**场景与交互：**
- 智能吸附：启用屏幕/任务栏/窗口吸附及检测距离
- 动画映射：为各场景（Idle、Screen-Top、Screen-Left、Screen-Right、Taskbar-Top、Window-Top 等）绑定动画
- 鼠标事件：开启/关闭点击、悬停、拖拽反馈
- 系统事件：订阅自定义事件（如时间段、系统提示、Dock 操作）

**扩展选项：**
- AI 对话预览：预留与 LLM 的文本对话接口
- 语音指令：预留麦克风输入与语音识别
- 摄像头动捕：预留摄像头授权与骨骼驱动映射
- 数据权限：统一管理本地/云端数据的隐私提示

**高级选项：**
- 置顶显示：保持伙伴窗口在最前端
- 穿透点击：允许鼠标事件穿透到桌面（仅观赏模式）
- Snap 可视化：显示吸附区域与检测框，便于调试
- 性能优化：限制动画检测频率，降低资源占用

### 4. 桌面小组件

**功能：** 实用的桌面信息显示

#### 可用小组件

**时钟小组件：**
- 数字/模拟时钟
- 多时区支持
- 12/24 小时制

**天气小组件：**
- 实时天气
- 7 天预报
- 温度、湿度、风速

**系统监控小组件：**
- CPU 使用率
- 内存使用率
- 磁盘空间
- 网络速度

**待办事项小组件：**
- 任务列表
- 完成/未完成标记
- 优先级设置

#### 添加小组件

1. 点击"小组件"设置
2. 点击"添加小组件"按钮
3. 选择小组件类型
4. 拖拽调整位置
5. 调整大小和设置

#### 管理小组件

- **移动**：拖拽小组件标题栏
- **调整大小**：拖拽小组件边角
- **设置**：右键小组件 → 设置
- **删除**：右键小组件 → 删除

---

## 设置与配置

### 通用设置

**语言：** 简体中文、English

**主题：**
- 浅色模式
- 深色模式
- 自动（跟随系统）

**启动选项：**
- 开机自启动
- 启动时最小化到托盘
- 记住窗口位置

### 配置文件

配置文件位于：
```
C:\Users\<用户名>\AppData\Roaming\com.aurora.desktop\config.json
```

**手动编辑配置：**
1. 关闭 Aurora
2. 编辑 `config.json`
3. 保存文件
4. 重新启动 Aurora

**导出配置：**
- 设置 → 通用 → 导出配置
- 选择保存位置

**导入配置：**
- 设置 → 通用 → 导入配置
- 选择配置文件

**恢复默认：**
- 设置 → 通用 → 恢复默认设置
- 或删除 `config.json` 重启

---

## 快捷键

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Alt + S` | 打开/关闭设置面板 |
| `Ctrl + Alt + D` | 显示/隐藏 Dock 栏 |
| `Ctrl + Alt + W` | 壁纸快速切换 |
| `Ctrl + Alt + P` | 显示/隐藏桌面伙伴 |
| `Ctrl + Alt + Q` | 退出 Aurora |

### 设置面板内

| 快捷键 | 功能 |
|--------|------|
| `Esc` | 关闭设置面板 |
| `Ctrl + S` | 保存当前设置 |
| `Ctrl + R` | 恢复默认设置 |
| `F5` | 刷新设置界面 |

### 自定义快捷键

1. 设置 → 通用 → 快捷键
2. 点击要修改的快捷键
3. 按下新的组合键
4. 点击"保存"

---

## 常见问题

### Q1: 如何卸载 Aurora？

**答：**
- Windows 设置 → 应用 → 应用和功能
- 找到 "Aurora" → 卸载
- 或使用安装包的卸载程序

### Q2: 壁纸设置后没有变化？

**答：**
1. 检查文件路径是否正确
2. 确认文件格式被支持
3. 查看错误提示信息
4. 尝试使用绝对路径
5. 重启应用

### Q3: 动态壁纸无法播放？

**答：**
1. 确认已安装 FFmpeg
   ```bash
   ffplay -version
   ```
2. 将 FFmpeg 添加到系统 PATH
3. 或修改配置使用绝对路径
4. 检查视频文件是否损坏
5. 尝试使用其他视频文件

### Q4: Dock 栏不显示？

**答：**
1. 检查 Dock 是否被隐藏（自动隐藏模式）
2. 按 `Ctrl + Alt + D` 切换显示
3. 检查 Dock 位置设置
4. 重启应用

### Q5: 桌面伙伴无法移动？

**答：**
1. 检查"穿透点击"是否启用（如果启用，无法点击）
2. 关闭"穿透点击"选项
3. 确认"自由移动"已启用
4. 检查窗口置顶设置

### Q6: 小组件不显示数据？

**答：**
1. 检查网络连接（天气小组件需要网络）
2. 查看小组件设置是否正确
3. 重启小组件
4. 查看控制台日志

### Q7: 应用占用内存过高？

**答：**
1. 关闭不需要的小组件
2. 停止动态壁纸（改用静态）
3. 减少 Dock 图标数量
4. 关闭桌面伙伴
5. 检查是否有内存泄漏（查看日志）

### Q8: 启动时报错？

**答：**
1. 检查系统要求（Windows 10/11）
2. 更新显卡驱动
3. 安装 WebView2（Windows 10 需要）
4. 查看日志文件获取详细错误
5. 尝试重新安装

---

## 故障排除

### 查看日志

**日志位置：**
```
C:\Users\<用户名>\AppData\Roaming\com.aurora.desktop\logs\
```

**日志文件：**
- `aurora.log` - 主日志
- `sidecar.log` - C++ 后端日志
- `error.log` - 错误日志

**开发模式日志：**
运行 `pnpm tauri:dev` 时，日志会直接显示在控制台。

### 完全重置

如果遇到严重问题，可以尝试完全重置：

1. **关闭 Aurora**

2. **删除配置文件：**
   ```
   C:\Users\<用户名>\AppData\Roaming\com.aurora.desktop\
   ```

3. **重新启动 Aurora**

应用会使用默认配置重新初始化。

### 收集诊断信息

报告问题时，请提供：

1. **系统信息**
   - 操作系统版本
   - Aurora 版本号
   - 显卡型号

2. **错误日志**
   - 复制 `logs/` 目录下的日志文件

3. **复现步骤**
   - 详细描述如何触发问题
   - 附上截图（如适用）

4. **配置文件**
   - `config.json` 的内容

### 联系支持

- **GitHub Issues**: https://github.com/MitsuhaFe/aurora/issues
- **Discord 社区**: 加入我们的 Discord 服务器
- **邮箱**: MitsuhaFe@gmail.com

---

## 性能优化建议

### 优化内存使用

1. 使用静态壁纸而非视频
2. 限制小组件数量（建议不超过 5 个）
3. 关闭不使用的功能
4. 定期重启应用

### 优化 CPU 使用

1. 降低视频壁纸分辨率
2. 减少小组件刷新频率
3. 关闭不必要的动画效果
4. 使用硬件加速

### 优化启动速度

1. 关闭"开机自启动"（按需启动）
2. 减少固定的 Dock 图标
3. 清理配置文件
4. 更新到最新版本

---

## 使用技巧

### 1. 创建壁纸播放列表

将常用壁纸路径保存，快速切换：

1. 收集你喜欢的壁纸
2. 创建快捷方式文件夹
3. 在设置中记录路径
4. 使用 `Ctrl + Alt + W` 快速切换

### 2. 多显示器设置

- 静态壁纸会应用到所有显示器
- 视频壁纸当前仅支持主显示器
- 小组件可以拖到任意显示器

### 3. 定时更换壁纸

使用 Windows 任务计划程序定时调用 Aurora 命令。

### 4. 自定义主题

编辑 CSS 文件自定义界面主题（高级用户）。

---

## 更新日志

查看 [CHANGELOG.md](CHANGELOG.md) 了解版本更新历史。

---

## 反馈与建议

我们非常重视你的反馈！

- **功能建议**: 在 GitHub Discussions 中讨论
- **Bug 报告**: 创建 GitHub Issue
- **使用心得**: 在社区分享你的体验

---

**版本：** v0.1.0  
**最后更新：** 2025-11-11  
**适用平台：** Windows 10/11

感谢使用 Aurora！ ✨
