# 🚀 立即开始使用 Aurora

## 当前状态

✅ **项目框架 100% 完成**  
✅ **前端页面 100% 完成**  
✅ **可立即运行查看效果**

---

## 📥 第一步：安装依赖

在项目根目录执行：

```bash
# 使用 pnpm（推荐，更快）
pnpm install

# 或使用 npm
npm install
```

**预计时间：** 2-5 分钟

---

## 🎨 第二步：启动开发服务器

### 方式 1：完整 Tauri 应用（推荐）

```bash
pnpm tauri:dev
```

**首次启动需要：**
- 编译 Rust 代码（约 2-3 分钟）
- 启动 Vite 开发服务器
- 打开 Tauri 窗口

**启动成功后：**
- 会自动打开 Aurora 应用窗口
- 可以看到精美的欢迎页面
- 可以导航到设置页面

### 方式 2：仅前端开发

```bash
pnpm dev
```

然后浏览器访问 `http://localhost:5173`

**适用场景：**
- 快速开发 UI
- 调试前端逻辑
- 不需要 Tauri 功能

---

## 🎯 查看效果

启动成功后，你可以：

### 1. 主页（/）
- ✨ 看到精美的渐变背景
- 🎨 查看 4 个功能介绍卡片
- 🖱️ 点击"开始配置"按钮

### 2. 设置页面（/settings）
- 📱 查看侧边栏导航
- ⚙️ 切换不同的设置面板
- 🔙 点击"返回主页"

### 3. 体验热重载
- 修改 `src/views/Home.vue` 的任何内容
- 保存文件
- 看到页面立即更新（无需刷新）

---

## 🛠️ 常用开发命令

```bash
# 启动 Tauri 开发模式
pnpm tauri:dev

# 仅启动前端
pnpm dev

# 代码格式化
pnpm format

# 代码检查
pnpm lint

# 构建项目
pnpm build

# 打包 Tauri 应用
pnpm tauri:build
```

---

## 📁 项目结构导航

### 前端文件位置

```
src/
├── views/
│   ├── Home.vue           ← 主页
│   └── Settings/
│       └── Index.vue      ← 设置页面
├── stores/
│   ├── appStore.ts        ← 应用状态
│   └── wallpaperStore.ts  ← 壁纸状态
├── composables/
│   └── useTauriBridge.ts  ← IPC 通信
├── styles/
│   └── main.css           ← 全局样式
└── router/
    └── index.ts           ← 路由配置
```

### 配置文件位置

```
├── package.json           ← 依赖和脚本
├── vite.config.ts         ← Vite 配置
├── tsconfig.json          ← TypeScript 配置
└── src-tauri/
    ├── tauri.conf.json    ← Tauri 配置
    └── Cargo.toml         ← Rust 依赖
```

---

## 🎨 开始开发

### 修改主页

编辑 `src/views/Home.vue`：

```vue
<!-- 修改标题 -->
<h1 class="title">
  <span class="gradient-text">你的应用名称</span>
</h1>

<!-- 修改功能介绍 -->
<div class="feature-card">
  <div class="icon">🌟</div>
  <h3>新功能</h3>
  <p>功能描述</p>
</div>
```

### 修改设置页面

编辑 `src/views/Settings/Index.vue`：

```typescript
// 添加新的菜单项
const menuItems: MenuItem[] = [
  { id: 'general', label: '通用设置', icon: '⚙️', description: '基本设置和偏好' },
  { id: 'your-new-tab', label: '新功能', icon: '🌟', description: '你的新功能' },
  // ...
];
```

### 修改全局样式

编辑 `src/styles/main.css`：

```css
/* 修改配色方案 */
:root {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}
```

---

## 🐛 常见问题

### 问题 1：依赖安装失败

```bash
# 清除缓存后重试
rm -rf node_modules
pnpm install
```

### 问题 2：Tauri 启动失败

**检查：**
1. Rust 是否安装？`rustc --version`
2. Tauri CLI 是否安装？
3. 查看错误日志

### 问题 3：页面空白

**检查：**
1. 浏览器控制台是否有错误
2. Vite 开发服务器是否启动
3. 端口 5173 是否被占用

---

## 📚 学习资源

### 官方文档
- [Vue 3 文档](https://cn.vuejs.org/)
- [Tauri 文档](https://tauri.app/zh-cn/)
- [Vite 文档](https://cn.vitejs.dev/)
- [TypeScript 文档](https://www.typescriptlang.org/zh/)

### 项目文档
- [快速开始指南](docs/快速开始指南.md)
- [详细开发文档](docs/Aurora开发文档（详细版）.md)
- [开发计划](docs/开发计划.md)

---

## ✅ 下一步

完成以上步骤后，你可以：

1. **熟悉代码结构** - 浏览各个文件
2. **修改 UI** - 尝试修改样式和内容
3. **添加功能** - 实现新的功能
4. **准备资源** - 添加图标和图片
5. **开发后端** - 开始 C++ 后端开发

---

<div align="center">
  <h2>🎉 现在就开始吧！</h2>
  <p>执行 <code>pnpm install && pnpm tauri:dev</code></p>
  <p>查看精美的 Aurora 界面 ✨</p>
</div>

---

**提示：** 如果你是第一次运行，启动可能需要几分钟，请耐心等待。

